.env

# Runtime state and secrets generated by Home Assistant
config/.storage/
config/secrets.yaml
config/*.db
config/*.db-*
config/*.log*
config/.cloud/
config/.HA_VERSION
config/deps/
config/tts/
config/backups/

# HACS and the custom components it installs (managed by install-hacs.sh / HACS)
config/custom_components/
config/www/community/
